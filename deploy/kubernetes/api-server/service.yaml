apiVersion: v1
kind: Service
metadata:
  name: api-server
  namespace: infrasense
  labels:
    app: api-server
    component: backend
spec:
  selector:
    app: api-server
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
