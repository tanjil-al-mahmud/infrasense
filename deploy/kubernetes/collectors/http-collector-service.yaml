apiVersion: v1
kind: Service
metadata:
  name: http-collector
  namespace: infrasense
  labels:
    app: http-collector
    component: collector
spec:
  selector:
    app: http-collector
  ports:
    - name: health
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
