apiVersion: v1
kind: Service
metadata:
  name: ipmi-collector
  namespace: infrasense
  labels:
    app: ipmi-collector
    component: collector
spec:
  selector:
    app: ipmi-collector
  ports:
    - name: health
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
