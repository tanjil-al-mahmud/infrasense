apiVersion: v1
kind: Service
metadata:
  name: redfish-collector
  namespace: infrasense
  labels:
    app: redfish-collector
    component: collector
spec:
  selector:
    app: redfish-collector
  ports:
    - name: health
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
