apiVersion: v1
kind: Service
metadata:
  name: snmp-collector
  namespace: infrasense
  labels:
    app: snmp-collector
    component: collector
spec:
  selector:
    app: snmp-collector
  ports:
    - name: health
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
