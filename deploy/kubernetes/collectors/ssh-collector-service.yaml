apiVersion: v1
kind: Service
metadata:
  name: ssh-collector
  namespace: infrasense
  labels:
    app: ssh-collector
    component: collector
spec:
  selector:
    app: ssh-collector
  ports:
    - name: health
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
