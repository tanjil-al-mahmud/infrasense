apiVersion: v1
kind: Service
metadata:
  name: discovery-engine
  namespace: infrasense
  labels:
    app: discovery-engine
    component: backend
spec:
  selector:
    app: discovery-engine
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
