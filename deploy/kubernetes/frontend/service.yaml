apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: infrasense
  labels:
    app: frontend
    component: frontend
spec:
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
