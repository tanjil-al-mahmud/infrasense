apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: infrasense
  labels:
    app: notification-service
    component: backend
spec:
  selector:
    app: notification-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
